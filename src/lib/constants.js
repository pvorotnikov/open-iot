const EVENTS = {
    BRIDGE_AWS_ENABLE: 'bridge.aws.enable',
    BRIDGE_AWS_DISABLE: 'bridge.aws.disable',
    BRIDGE_OUT: 'bridge.out',
    BRIDGE_IN: 'bridge.in',
    MODULE_ENABLE: 'module.enable',
    MODULE_DISABLE: 'module.disable',
    MQTT_PUBLISH_MESSAGE: 'mqtt.publish.message',
}

module.exports = {
    EVENTS,
}
